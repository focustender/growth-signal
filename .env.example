# Copy to .env and fill in real values. Never commit .env itself.

# Salesforce Developer Edition (developer.salesforce.com)
# OAuth 2.0 username-password flow via a Connected App (Setup > App Manager
# > New Connected App > Enable OAuth Settings > Manage Consumer Details).
# SOAP API login() is disabled by default on new orgs, so plain
# username+password+security_token alone will fail -- consumer_key/secret
# are required.
SALESFORCE_USERNAME=
SALESFORCE_PASSWORD=
SALESFORCE_SECURITY_TOKEN=
SALESFORCE_CONSUMER_KEY=
SALESFORCE_CONSUMER_SECRET=
SALESFORCE_DOMAIN=login

# HubSpot — only needed as a fallback if the Claude Code HubSpot MCP
# read tools aren't available in your environment. Create under
# HubSpot Settings > Integrations > Private Apps, with CRM read scopes.
HUBSPOT_PRIVATE_APP_TOKEN=
